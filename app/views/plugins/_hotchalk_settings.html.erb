<% fields_for :settings, OpenObject.new(settings) do |f| %>
  <table style="width: 500px;" class="formtable">
    <tr>
      <td><%= f.blabel :cl_base_url, :en => "Course Library Base URL" %></td>
      <td><%= f.text_field :cl_base_url%></td>
    </tr>
  </table>
<% end %>
